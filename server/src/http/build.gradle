plugins {
    id 'application'
}

sourceSets {
    main {
        java {
            srcDirs = ["."]
        }
    }
}

dependencies {
    implementation project(':src:game')
}

buildDir = "../../build/http"

application {
    mainClass = 'HTTPManager'
}