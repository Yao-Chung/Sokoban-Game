plugins {
    id 'application'
}

sourceSets {
    main {
        java {
            srcDirs = ["."]
        }
    }
}

tasks.run.workingDir = System.getProperty("user.dir")
buildDir = "../../build/game"

application {
    mainClass = 'GameManager'
}